<div  class="admin_template_form">
<%= form_for(@blog_template) do |f| %>
  <% if @blog_template.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@blog_template.errors.count, "error") %> prohibited this task from being saved:</h2>

      <ul>
      <% @blog_template.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :body %><br />
    <%= f.text_field :body %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
</div>
