<div class="contents">
<h2>タスク共有サービス。やることを整理して効率用仕事しましょう</h2>
<% if current_user %>
<%= render 'common/login_info' %>
<menu class="menu_text">
  <%=link_to 'Myタスク一覧', :action =>'index' ,:controller=>'jobs' %>&nbsp;&nbsp;|&nbsp;&nbsp; 
</menu>
<%= render 'content' %>
<% else %>
<menu class="menu_text">
  <%= link_to 'Twitterログイン', "/auth/#{if Rails.env.production? then 'twitter' else  'developer' end}" %>&nbsp;&nbsp;|&nbsp;&nbsp;
</menu>
<% end %>
</div>
