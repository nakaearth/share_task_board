<div class="content">
<table >
  <th class="list_menu">概要</th>
  <th></th>
  <th></th>
  <th></th>
  <% @jobs.each do |job| %>
  <tr class="task_css<%=job.priority %>">
    <td><%=task.title %>
    <td ><%=status_str(job.status) %></td>
    <td><%= link_to '編集する', edit_task_path(job) %></td>
  </tr> 
  <% end %>
</table>
</div>
